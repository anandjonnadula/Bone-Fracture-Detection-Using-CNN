# Pinned, known-good versions (Python 3.11–3.13, CPU TensorFlow).
# NOTE (Windows): create the virtualenv at a SHORT path, e.g.
#   python -m venv %USERPROFILE%\.venvs\bfd
# Installing TensorFlow into a venv nested deep inside this project exceeds
# the Windows 260-character path limit and produces a broken install.
# (Or skip all of that and use Docker: `docker compose up`.)
tensorflow==2.20.0
flask==3.1.3
numpy==2.5.1
pillow==12.3.0
# Headless build: no GUI libs needed server-side (keeps Docker image lean).
opencv-python-headless==5.0.0.93
scikit-learn==1.9.0
scipy==1.18.0
reportlab==5.0.0
# Security: CSRF protection + rate limiting
flask-wtf==1.3.0
flask-limiter==4.1.1
# DICOM support (pylibjpeg handles compressed transfer syntaxes)
pydicom==3.0.2
pylibjpeg==2.1.0
pylibjpeg-libjpeg==2.4.0
# Only needed to load the ORIGINAL legacy .h5 models for baseline comparison;
# safe to remove once you use the retrained .keras models.
tf_keras==2.20.1
