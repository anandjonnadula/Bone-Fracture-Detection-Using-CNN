# Fracture-localization detector (Stage 1.5) — see docs/TRAINING.md.
# Runtime integration lives in infer_detector.py and degrades gracefully
# when weights are absent (the pipeline simply skips localization).
